server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # SPA history fallback so deep links resolve on refresh.
  location / {
    try_files $uri $uri/ /index.html;
  }

  # Proxy API calls to the backend service (compose service name).
}
